// it's about conditional statements (if)

var a = 5;
var b = 5;
var theNumbersMatch;

// to test if two things are identical, you can use '===' at the condition parameter
if (a == b) {
    theNumbersMatch = true;
} else {
    theNumbersMatch = false;
}

console.log("The numbers match: " + theNumbersMatch);